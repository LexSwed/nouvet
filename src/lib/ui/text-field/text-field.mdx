import { Canvas, Meta } from '@storybook/blocks';

import * as TextFieldStories from './text-field.stories.tsx';

<Meta of={TextFieldStories} />

# TextField

Wraps HTML `<input />` element, supporting native props + stylized `label` and supporting text, with enhanced accessibility.

<Canvas of={TextFieldStories.Primary} />

## Validity states

